<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp" >

    <!-- Username -->

    <TextView
        android:id="@+id/textview_label_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Username"
        android:textAppearance="?android:attr/textAppearanceLarge" >
    </TextView>

    <EditText
        android:id="@+id/edittext_username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/textview_label_username"
        android:layout_marginTop="14dp"
        android:ems="10"
        android:hint="Enter account username"
        android:inputType="textShortMessage"
        android:text="floring" >

        <requestFocus />
    </EditText>

    <!-- Repository -->

    <TextView
        android:id="@+id/textview_label_repository"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/edittext_username"
        android:layout_marginTop="25dp"
        android:text="Repository Name"
        android:textAppearance="?android:attr/textAppearanceLarge" >
    </TextView>

    <EditText
        android:id="@+id/edittext_repository"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/textview_label_repository"
        android:layout_marginTop="14dp"
        android:ems="10"
        android:hint="Enter repository name you want to check"
        android:inputType="textShortMessage"
        android:text="RepositorySentry" >

        <requestFocus />
    </EditText>

    <!-- Set, Clear Buttons -->

    <Button
        android:id="@+id/button_clear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:text="Clear" />

    <Button
        android:id="@+id/button_set_alarm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="Set" />

</RelativeLayout>